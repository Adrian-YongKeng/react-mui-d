import { Box, Typography } from "@mui/material";

const Profile = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Profile
      </Typography>

      <Typography variant="body1">
        This is the Profile page, where you can manage your user settings.
      </Typography>
    </div>
  );
};

export default Profile;
